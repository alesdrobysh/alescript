// Fibonacci sequence in alescript

recipe fibonacci(n)
    brew a from water.
    brew b from water, barley.
    wait for 1 day.
    brew temp from water.

    repeat n times:
        relabel temp as a.
        relabel a as b.
        blend b with temp.
    end.

    a
end.

toast fibonacci(10).
